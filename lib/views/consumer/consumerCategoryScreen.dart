import 'package:flutter/material.dart';

class ConsumerCategoryScreen extends StatelessWidget {
  const ConsumerCategoryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(color: Color.fromRGBO(253, 230, 27, 1),);
  }
}